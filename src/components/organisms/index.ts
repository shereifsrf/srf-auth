/**
 * Organisms Index
 * Export all organism components (both headless and styled)
 */

export { AuthCard, default as StyledAuthCard } from "./AuthCard";

// Export types
export type { AuthCardProps } from "./AuthCard";
